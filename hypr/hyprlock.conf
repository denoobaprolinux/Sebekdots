# Esquema de colores de Pywal

source = /$HOME/.config/hypr/matugen/hyprland-colors.conf

# Fondo de Pantalla generado por swww

background {
    monitor =
    path = /$HOME/.cache/current_wallpaper.png
    
}

# Campo de Contraseña

input-field {
    monitor =
    size = 200, 30
    outline_thickness = 3
    dots_size = 0.25 # Rango de tamaño de los puntos entre 0.2 - 0.8
    dots_spacing = 0.3 # Espaciado de los puntos entre 0.0 - 1.0
    dots_center = true
    outer_color = $primary 
    inner_color = $shadow
    font_color = rgb(200, 200, 200)
    fade_on_empty = true
    hide_input = false

    position = 0, -350
    halign = center
    valign = center
}

# Mensaje en la Pantalla

#label {
#    monitor =
#    text = hola, $USER # Modifica esto a tu gusto
#    color = $color1
#    font_size = 30
#    font_family = kamikaze # Modifica esto a tu gusto

#    position = 0, -300
#    halign = center
#    valign = center
#}

label {
    
    monitor =
    text = cmd[update:1000] echo "$(date +"%-I:%M%p")"
    color = $outline
    font_size = 70
    font_family = fast hand
    position = 100, 0 
}
